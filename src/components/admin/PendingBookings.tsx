import React from 'react';

const PendingBookings: React.FC = () => {
  return (
    <div>
      <h2>This is the Pending Bookings component - replace this with booking approval interface</h2>
      <p>List of pending bookings with approve/reject actions will go here</p>
      <p>Add booking details, approval workflow, and notification system</p>
    </div>
  );
};

export default PendingBookings;
